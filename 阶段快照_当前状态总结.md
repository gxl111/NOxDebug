# NOxDebug 项目阶段快照

> **快照时间**：2026-05-31
> **说明**：本快照仅基于当前代码仓库实际文件状态整理，独立描述该阶段交付内容。

---

## 1. 项目定位与硬件基础

- **MCU**：STM32F103RCT6（72 MHz，HSE 8 MHz + PLL×9）
- **RTOS**：FreeRTOS + CMSIS-RTOS v2
- **开发环境**：Keil µVision（MDK-ARM），由 STM32CubeMX 生成基础工程

**三路传感器接入方式**：
- **ch0（S1）**：片载 CAN1，J1939 SA = `0x52`（出口）
- **ch1（S2）**：片载 CAN1，J1939 SA = `0x51`（入口）
- **ch2（S3）**：SPI2 + MCP2515 第二路 CAN，J1939 扩展 ID = `0x18F00F52`（SA 0x52）

两个 SA `0x52` 不冲突的原因：物理隔离为两条 CAN 总线，软件按“从哪路 CAN 收到”区分入队通道。

**关键外设**：
- CAN1：250 kbps J1939，接 ch0/ch1
- MCP2515（SPI2）：250 kbps，PB13/14/15 + PC10(CS) + PC11(INT)，仅接收 `0x18F00F52`
- USART1：Modbus 从站 RS485，默认 115200
- UART5：Modbus 主站 RS485，固定 9600，接外部 4–20 mA 输出模块
- I2C1：OLED（PB6/PB7）
- SPI1：SD 卡（FATFS）

---

## 2. 软件架构与任务划分

| 任务 | 职责 |
|------|------|
| `defaultTask` | 占位 |
| `NOx_Receive` | 出队 J1939 帧（含通道索引 0/1/2），更新每通道数据；执行工作模式策略；更新 P01/P02/P07；填充 4–20 mA 缓冲；发送加热命令（片载 CAN + MCP2515）；OLED 刷新（若 `APP_USE_OLED=1`） |
| `NOx_Default` | 标定轮询（每通道）、报警、反吹逻辑（3 路）、运行时间、OLED show |
| `ModBus_Slave` | 初始化（含 MCP2515、Flash 加载/出厂编程）、Modbus 从站轮询 |
| `ModBus_Host` | 周期将 4–20 mA 码写入外部从站（10H + 03H 读回校验） |

**定时器**：TIM6（HAL 节拍）、TIM7（10 ms 基准）、TIM2/TIM3（Modbus 从站/主机 RX 超时）。

---

## 3. 核心功能现状

### 3.1 传感器数据通道（nox_channel）

- `NoxChannel_t` 结构体封装每通道的 raw/ppm/state/valid、标定段参数、标定点数组
- `NoxChannel_UpdateFromCan()`：解析 8 字节 J1939 载荷，按 Status Byte 的 2-bit 字段判定有效位
- `NoxChannel_UpdateCommTimeouts()`：**通信超时检测**（`NOX_CAN_SILENCE_TIMEOUT_MS = 2000 ms`），超时后置 `valid=0`，状态字置 `0x0300`（bit8+bit9），bit9 为链路丢失标志
- **反吹中冻结**：某路反吹时，该路 raw/ppm/state/valid 冻结为反吹前最后值，但仍刷新 `last_rx_ms`，不做静默超时改写

### 3.2 工作模式策略（P34，40012）

- **模式 0（单路）**：高字节选通道 0/1/2，该路驱动 P01/P02/P07 与 4–20 mA；若该路无效或反吹，自动降级找其他有效路
- **模式 1（主备，默认）**：高字节选主通道；优先主通道，主无效/反吹时用其他有效路；反吹时自动切换到未反吹路
- **模式 2（融合）**：≥2 路有效且未反吹时，NOx/O₂ 取平均；仅 1 路有效时降级为单路逻辑
- **P35（40013，只读）**：`0b01=S0, 0b10=S1, 0b11=融合, 0b100=S2, 0b00=故障`

### 3.3 标定（calibration）

- **每传感器独立**：NOx / O₂ 各 3 点、两段线性，无全局“标定目标”
- 标定点 2/3 的参考值（Y）存于各传感器块；点选择 + 触发寄存器也在各块内
- 触发值：`0x0001` = 执行一步（成功回 `0x000F`，失败回 `0x0005`）；`0x0002` = 恢复默认（成功回 `0x0010`）
- 标定成功后调用 `InternalFlash_Write()` 保存；`FactoryFlash_ProgramDefaults()` 用于出厂写 Flash

### 3.4 反吹（blowback）

- **3 路均支持反吹定时**：每路独立间隔/时长/命令寄存器
- **同一时间仅一路反吹**（硬件保护，包括融合模式下）
- **错峰策略**：ch0 在 `tick % span == 0` 触发；ch1 在 `tick % span == stagger(300s)` 触发；ch2 在 `tick % span == span*2/3` 触发
- 反吹时自动关对应抽气阀（J1/J4/J7），开反吹阀（J2/J5/J8）；结束按寄存器恢复
- ch2 反吹无实际继电器动作（仅有寄存器/定时器状态），但逻辑完整保留

### 3.5 阀门控制（已重构）

- **9 路继电器 J1–J9**：每传感器一组（正常抽气、反吹、校准）
- **控制方式**：由**保持寄存器**（40053–40055、40095–40097、40137–40139）R/W 控制，`0=关 1=开`
- **线圈 D01–D04 已废弃**：仍支持 01H/05H 读写，但写线圈不再驱动任何 GPIO
- 自动抽气逻辑：`Modbus_AutoSuctionValvesUpdate()` 在通道正常（bit9 未置位、非反吹、校准阀关）时自动置抽气阀

### 3.6 传感器上电 GPIO（预留）

- `app_config.h` 中 `SENSOR_POWER_GPIO_ENABLE = 0`（默认禁用）
- 引脚已预留：S1→PC0、S2→PC13、S3→PB9
- 上电寄存器（40014、40056、40098）始终可读写；使能 GPIO 后写 1/0 驱动对应引脚高低

---

## 4. Modbus 寄存器映射现状

**总布局**：公共（40001–40013）→ S1 块（40014–40055，42 字）→ S2 块（40056–40097，42 字）→ S3 块（40098–40139，42 字），共 **139 字**。

| 区域 | 地址范围 | 说明 |
|------|----------|------|
| 公共输出 | 40001–40002 | P01 NOx（float，策略输出） |
| 公共输出 | 40003–40004 | P02 O₂（float，策略输出） |
| 公共输出 | 40005 | P07 状态字（9 位 J1939 + bit9 超时） |
| 报警阈值 | 40006–40007 | P12 NOx 高报警（float） |
| 报警阈值 | 40008–40009 | P13 O₂ 低报警（float） |
| 4–20 mA | 40010–40011 | P22/P23 NOx/O₂ 码（u16） |
| 工作模式 | 40012 | P34 模式（低字节 0/1/2）+ 通道选择（高字节） |
| 输出源 | 40013 | P35 只读（0b01/0b10/0b11/0b100/0b00） |
| S1 块 | 40014–40055 | 上电/实时/标定/反吹/阀门（J1/J2/J3） |
| S2 块 | 40056–40097 | 同上（J4/J5/J6） |
| S3 块 | 40098–40139 | 同上（J7/J8/J9） |

每传感器块内结构完全一致：
- 偏移 0：上电（R/W）
- 偏移 1–2：实时 NOx（float，R）
- 偏移 3–4：实时 O₂（float，R）
- 偏移 5：状态字（R）
- 偏移 6–13：段 1 斜率截距（4×float，R/W）
- 偏移 14–21：段 2 斜率截距（4×float，R/W）
- 偏移 22–29：标定点 2/3（4×float，R/W）
- 偏移 30–33：NOx/O₂ 点选择 + 标定触发（R/W）
- 偏移 34–38：反吹间隔/时长/状态/倒计时/命令（R/W + R）
- 偏移 39–41：阀门 正常抽气/反吹/校准（R/W）

---

## 5. 关键配置常量（app_config.h）

| 常量 | 当前值 | 说明 |
|------|--------|------|
| `NOX_SENSOR_COUNT` | 3 | 当前激活 3 路传感器 |
| `NOX_SENSOR_COUNT_MAX` | 3 | 最大支持 3 路 |
| `NOX_USE_MCP2515` | 1 | 第二路 CAN（ch2）已启用 |
| `DEFAULT_BLOW_DURATION` | 60 s | 默认反吹时长 |
| `DEFAULT_BLOW_INTERVAL` | 3600 s | 默认反吹间隔 |
| `BLOW_STAGGER_SEC` | 300 s | ch1 相对 ch0 错峰 |
| `NOX_CAN_SILENCE_TIMEOUT_MS` | 2000 ms | CAN 静默超时 |
| `NOX_FS_PPM` | 2500.0f | 4–20 mA NOx 满量程 |
| `O2_FS_PCT` | 25.0f | 4–20 mA O₂ 满量程 |
| `FACTORY_FLASH_PROGRAM_ON_BOOT` | 0 | 正常启动：从 Flash 加载参数 |
| `SENSOR_POWER_GPIO_ENABLE` | 0 | 传感器上电 GPIO 默认不驱动 |
| `APP_USE_OLED` | 1 | 编译并周期性刷新 OLED |

---

## 6. 已知特点与注意事项

1. **MCP2515 初始化**：`ModBus_Slave` 任务中初始化，设置硬件滤波仅接收 `0x18F00F52`；同时发送加热命令 `0x18FEDF55`
2. **Flash 操作**：`FactoryFlash_ProgramDefaults()` 仅在 `FACTORY_FLASH_PROGRAM_ON_BOOT=1` 时执行，正常为 0 避免每次上电覆盖用户标定
3. **OLED 显示**：`NOx_Receive` 任务中每 50 ms 刷新三行传感器数据 + 输出行 + 状态行；`NOx_Default` 中刷新运行时间并 `OLED_ShowFrame()`
4. **反吹倒计时逻辑**：空闲时显示距离下次周期反吹的秒数；反吹中时显示剩余秒数
5. **ch2 的特殊性**：作为第二路 CAN（MCP2515）接入，SA 也是 0x52，但软件通过接收路径区分；反吹无实际继电器，但寄存器/定时器逻辑完整
6. **状态字 bit9**：通信超时标志（0x0200），与低 9 位 J1939 状态组合使用；正常未超时且传感器全好时值为 `0x01FF`
7. **03H 单次读取限制**：125 字；若主站轮询全部 139 字，需分两次（如 40001×125 + 40126×14）

---

## 7. 文件变更焦点（本阶段）

本阶段代码已具备**完整的三路传感器支持**（从原先的双路扩展而来），核心变更集中在：
- `nox_channel.c/h`：通道数组从 2 路扩展到 3 路，策略逻辑（单路/主备/融合）兼容 3 路
- `blowback.c/h`：增加 ch2 反吹配置接口，3 路错峰逻辑，反吹中冻结测量值
- `NOx.c`：MCP2515 初始化与收发放到 `ModBus_Slave` 和 `NOx_Receive` 任务；Register_Init/AfterFlash_Init 覆盖 3 路
- `modbus_slave.c/h`：扩展 S3 寄存器块（40098–40139），阀门控制从线圈迁移到保持寄存器
- `app_config.h`：`NOX_SENSOR_COUNT` 设为 3，新增 `NOX_SENSOR_COUNT_MAX` 等宏
- `README.md`：完整重写，反映 3 路、线圈废弃、阀门寄存器、通信超时、上电 GPIO 等新逻辑

---

*快照结束。下一阶段可能的演进方向：现场调试反馈优化、标定流程完善、新增 HMI/SCADA 对接需求、报警输出继电器扩展等。*
